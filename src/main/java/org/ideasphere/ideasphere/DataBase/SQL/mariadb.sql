-- MariaDB 初始化脚本

CREATE TABLE IF NOT EXISTS sample_table (
                                            id INT PRIMARY KEY AUTO_INCREMENT,
                                            name VARCHAR(255)
    );